version: 2.1
orbs:
  cypress: cypress-io/cypress@1
workflows:
  build:
    jobs:
      - cypress/install
      - cypress/run:
          group: '4x test'
          requires:
            - cypress/install
          parallel: true
          parallelism: 4
          start: 'npm run test:ci'
